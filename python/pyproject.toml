[tool.poetry]
name = "face-service"
version = "2.0.0"
description = "MediaPipe face analysis + YOLOv8 object detection service"
authors = ["Siddharth"]

[tool.poetry.dependencies]
python = "^3.10"
opencv-python-headless = "^4.9.0"
numpy = "^1.26.0"
mediapipe = "0.10.14"
flask = "^3.0.0"
flask-cors = "^4.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
